; =============================================================================
; Native SPANN .ini build config -- SPACEV-1B, attribute-aware (multi-tenant),
; in-posting OPQ-25, all three unfilter-enhancement layers ON.
;
;   Run:  Tools/benchmarks/run_spann_attr_build.sh \
;             Tools/benchmarks/build_spann_attr_spacev1b_opq25.ini
;         (internally: Release/spannbuilder -c <this file>  +  augmentheadgraph)
;
; This is the BILLION-scale sibling of build_spann_attr_spacev_opq25.ini (3M).
; It is the single source of truth for the 1B build, in SPANN's native sectioned
; .ini format (read by Helper::IniReader). See AGENTS.md -> "Build Config --
; Native .ini" for the full key->engine mapping. Comments MUST start with ';';
; inline comments are NOT stripped, so value lines carry ONLY the value.
;
; -----------------------------------------------------------------------------
; REPRODUCIBILITY -- the three derived builder inputs referenced below are NOT
; in the repo (they are 20-25 GB each). Regenerate them deterministically with:
;
;   Tools/benchmarks/prep_spacev1b_inputs.sh        ; full 1B  (or pass N for a smoke subset)
;
; Pure C++ (no Python): the script drives spannbuilder subcommands that mimic
; AnnService/src/Quantizer/main.cpp (IQuantizer::LoadIQuantizer + QuantizeVector),
; so the artifacts are byte-for-byte what the build/search trust:
;   --merge-tags5   tags.npy[1B,4]+num_attr.npy[1B] -> spacev1b_tags5.u32 [1B,5]
;   --gen-opq-codes base i8bin + 3M-trained codebook -> opq_codes_m25.bin
;                   (raw widen, ADC=false, header-less N*M; the in-posting
;                    convention -- NOT the normalizing generic Release/quantizer)
; Outputs land under /mnt/nvme/baotonglu/mocheng/datasets/spacev1b/spacev1b_build/ (+ a copy of the
; 3M-trained opq_quantizer.bin for search-time ADC).
; Source dataset (committed): /home/v-mochengli/datasets/big-ann/MSSPACEV1B/
;   spacev1b_base.i8bin [1B,100] int8  +  multitenant/{tags.npy,num_attr.npy}
; =============================================================================

[Base]
; Native DEFAULT reader validates the (n,d) int32 header and payload.
VectorPath=/mnt/nvme/baotonglu/mocheng/datasets/spacev1b/spacev1b_base.i8bin
Dim=100
ValueType=Int8
VectorType=DEFAULT
DistCalcMethod=L2
Normalized=true
IndexDirectory=/mnt/nvme/baotonglu/mocheng/datasets/spacev1b/spacev1b_opq25

[Tags]
; 5 uint32 cols/vec: [org,dept,team,project] (ACL) + price (numeric).
TagFile=/mnt/nvme/baotonglu/mocheng/datasets/spacev1b/spacev1b_build/spacev1b_tags5.u32
NumTagsPerVec=5
ColumnTypes=categorical,categorical,categorical,categorical,numeric

[Build]
BuildSignatures=true

[SelectHead]
DualPoolAugment=0
DualPoolExtraRatio=0.1
; One global spatial BKT; Ratio = final head fraction.
SelectHeadType=BKT
Ratio=0.1
; Pin the canonical BKT balance factor. A non-negative value skips the expensive
; DynamicFactorSelect sweep; 100 is the reference SPANN setting used by the
; attribute-aware SIFT/SPACEV configurations.
BKTLambdaFactor=100
; Multi-threaded parallel BKT build (level-order sibling-node parallelism).
; The global spatial tree is large, so parallelism inside the tree matters. Use ~2/3 of
; the 96-core box (resources coordinated externally) so BuildTreesParallel saturates.
ParallelBKTBuild=true
NumberOfThreads=64

[BuildHead]
NumberOfThreads=64

[BuildSSDIndex]
CrossEdges=1
CrossExtraEdges=10
; native SPANN posting-store params (applied via SetSSDBuildParam)
Storage=FILEIO
; ~2/3 of the 96-core box (resources coordinated externally).
NumberOfThreads=64
ReplicaCount=8
; unfilter-tail: native SSD params (single source of truth, no env bridge).
;   TailReplicaCount       = K nearest-head tail copies/vector (was UnfilterTailKReplica)
;   UnfilterTailBufferLength = extra per-posting tail buffer pages (was UnfilterTailBufferPages)
TailReplicaCount=8
UnfilterTailBufferLength=1
; in-posting OPQ quantization: posting record = [meta | 25-byte OPQ code]
PostingQuantizer=OPQ
PostingQuantM=25
PostingQuantizerFile=/mnt/nvme/baotonglu/mocheng/datasets/spacev1b/spacev1b_build/opq_codes_m25.bin
; cold-rerank full-precision base (the 1B i8bin) + survivor depth
FullVectorFile=/mnt/nvme/baotonglu/mocheng/datasets/spacev1b/spacev1b_base.i8bin
RerankL=150
; SSD block-pool sizing (GB), pinned explicitly (bypasses the auto estimate).
; Slim record ~= 25 + 5*4 + 32 = 77 B; 1B*77*~5 replicas ~= 388 GB real posting
; data. Start=420/Max=560 fits our 7 TB md0 NVMe (5.7 TB free) alongside the head index.
StartFileSizeGB=420
MaxFileSizeGB=560

[SearchSSDIndex]
; Persist the unfilter-tail read policy with the index. Filtered queries still
; scan pure prefixes only; FileIO posting prefilter remains opt-in.
EnableUnfilterTail=true

[MultiTenant]
; Record schema: categorical columns 0-3 and one trailing numeric value.
; Unfilter-enhancement layers. Cross-graph and unfilter-tail stay on; U_extra is
; switchable here and defaults OFF after ablation showed no recall gain once H1
; tail replicas are enabled. Set DualPoolAugment=1 to build U_extra/head_role.bin.
;   CrossEdges=1       -> stitch per-node head bundles (head_cross_edges.bin)
;   CrossEdges=0       -> skip; unfilter falls back to per-node fan-out
;   CrossExtraEdges=N  -> cross-subgraph edges kept per head (-m; <=0 -> 10)
; In-place build: write the SPANN index DIRECTLY into IndexDirectory/tenant_0 instead
; of staging in /tmp and copying at the end. Avoids the transient 2x disk footprint
; (work dir + final dir) and the final copy -- essential at billion scale. The SSD
; block pool (StartFileSizeGB) is pre-allocated in the final dir, so IndexDirectory's
; filesystem must have room for it (here /mnt/nvme, 420-560GB of 5.7TB free).
InPlaceBuild=1
