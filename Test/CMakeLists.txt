# Copyright (c) Microsoft Corporation. All rights reserved.
# Licensed under the MIT License.

if (NOT LIBRARYONLY)
    if(NOT WIN32)
      ADD_DEFINITIONS(-DBOOST_TEST_DYN_LINK)
      message (STATUS "BOOST_TEST_DYN_LINK")
    endif()

    find_package(Boost 1.66 COMPONENTS system thread serialization wserialization regex filesystem unit_test_framework)
    if (Boost_FOUND)
        include_directories (${Boost_INCLUDE_DIR})
        link_directories (${Boost_LIBRARY_DIR})
        message (STATUS "Found Boost.")
        message (STATUS "Include Path: ${Boost_INCLUDE_DIRS}")
        message (STATUS "Library Path: ${Boost_LIBRARY_DIRS}")
        message (STATUS "Library: ${Boost_LIBRARIES}")
    else()
        message (FATAL_ERROR "Could not find Boost 1.67!")
    endif()

    include_directories(${PROJECT_SOURCE_DIR}/AnnService ${PROJECT_SOURCE_DIR}/Test ${PROJECT_SOURCE_DIR}/Wrappers ${PROJECT_SOURCE_DIR}/ThirdParty/spdk/build/include)

    file(GLOB TEST_HDR_FILES ${PROJECT_SOURCE_DIR}/Test/inc/Test.h)
    file(GLOB TEST_SRC_FILES ${PROJECT_SOURCE_DIR}/Test/src/*.cpp ${PROJECT_SOURCE_DIR}/Wrappers/src/CoreInterface.cpp)
    add_executable(SPTAGTest ${TEST_SRC_FILES} ${TEST_HDR_FILES})
    target_link_libraries(SPTAGTest SPTAGLibStatic ssdservingLib ${Boost_LIBRARIES})
    add_executable(NativePostingTest NativePostingTest.cpp)
    target_link_libraries(NativePostingTest SPTAGLibStatic ${Boost_LIBRARIES})
    add_test(NAME NativePosting.Navigation COMMAND NativePostingTest)
    add_executable(SearchBudgetTest SearchBudgetTest.cpp)
    target_link_libraries(SearchBudgetTest SPTAGLibStatic ${Boost_LIBRARIES})
    add_test(NAME NativePosting.SearchBudgets COMMAND SearchBudgetTest)
    set(SPTAG_PINNED_BKT_REFERENCE_DIR "" CACHE PATH "Authenticated upstream method extraction")
    if(SPTAG_PINNED_BKT_REFERENCE_DIR)
      add_executable(BKTReferenceTest BKTReferenceTest.cpp)
      target_include_directories(BKTReferenceTest PRIVATE "${SPTAG_PINNED_BKT_REFERENCE_DIR}")
      target_link_libraries(BKTReferenceTest SPTAGLibStatic ${Boost_LIBRARIES})
    endif()
    if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
      add_executable(SearchBudgetUBSan SearchBudgetTest.cpp)
      target_compile_options(SearchBudgetUBSan PRIVATE -fsanitize=undefined -fno-sanitize-recover=undefined)
      target_link_libraries(SearchBudgetUBSan SPTAGLibStatic ${Boost_LIBRARIES} -fsanitize=undefined)
      add_test(NAME NativePosting.SearchBudgetsUBSan COMMAND SearchBudgetUBSan)
    endif()
    add_executable(NumericSignatureTest NumericSignatureTest.cpp)
    add_test(NAME NativePosting.NumericRanges COMMAND NumericSignatureTest)
    add_executable(PostingCollectionTest PostingCollectionTest.cpp)
    target_link_libraries(PostingCollectionTest SPTAGLibStatic ${Boost_LIBRARIES})
    add_test(NAME NativePosting.Collection COMMAND PostingCollectionTest)
    add_executable(PostgraphPostingTest PostgraphPostingTest.cpp)
    target_link_libraries(PostgraphPostingTest SPTAGLibStatic ${Boost_LIBRARIES})
    add_test(NAME NativePosting.Postgraph COMMAND PostgraphPostingTest)
    add_executable(PostgraphAliasTest PostgraphAliasTest.cpp)
    target_link_libraries(PostgraphAliasTest SPTAGLibStatic ${Boost_LIBRARIES})
    add_test(NAME NativePosting.PostgraphAliases COMMAND PostgraphAliasTest)
    add_executable(CompactMetadataTest CompactMetadataTest.cpp)
    target_link_libraries(CompactMetadataTest SPTAGLibStatic ${Boost_LIBRARIES})
    add_test(NAME NativePosting.CompactMetadata COMMAND CompactMetadataTest)
    if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang")
      add_executable(CompactMetadataUBSan CompactMetadataTest.cpp)
      target_compile_options(CompactMetadataUBSan PRIVATE -fsanitize=undefined -fno-sanitize-recover=undefined)
      target_link_libraries(CompactMetadataUBSan SPTAGLibStatic ${Boost_LIBRARIES} -fsanitize=undefined)
      add_test(NAME NativePosting.CompactMetadataUBSan COMMAND CompactMetadataUBSan)
      add_executable(NativePostingUBSan NativePostingTest.cpp)
      target_compile_options(NativePostingUBSan PRIVATE -fsanitize=undefined -fno-sanitize-recover=undefined)
      target_link_libraries(NativePostingUBSan SPTAGLibStatic ${Boost_LIBRARIES} -fsanitize=undefined)
      add_test(NAME NativePosting.VisitedUBSan COMMAND NativePostingUBSan --storage-only)
      add_executable(PostingCollectionUBSan PostingCollectionTest.cpp)
      target_compile_options(PostingCollectionUBSan PRIVATE -fsanitize=undefined -fno-sanitize-recover=undefined)
      target_link_libraries(PostingCollectionUBSan SPTAGLibStatic ${Boost_LIBRARIES} -fsanitize=undefined)
      add_test(NAME NativePosting.CollectionUBSan COMMAND PostingCollectionUBSan)
    endif()
    add_test(NAME NativePosting.Catalogs COMMAND SPTAGTest --run_test=HierarchyCatalogTest)
    add_test(NAME NativePosting.Persistence
      COMMAND SPTAGTest --run_test=SPFreshTest/HierarchySharedRatioFiveLevelsRoundTrip)

    add_test(
      NAME SPTAGTest.ACLConcurrentSearchWithThreadLocalContext
      COMMAND SPTAGTest --run_test=ACLConcurrentTest/SearchWithACLSameTenantThreadLocalState
    )

    install(TARGETS SPTAGTest
      RUNTIME DESTINATION bin  
      ARCHIVE DESTINATION lib
      LIBRARY DESTINATION lib)
endif()
